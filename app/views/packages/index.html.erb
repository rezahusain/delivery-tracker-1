<div>
  <div>
    <h1>
      Delivery Tracker
    </h1>
  </div>
</div>
<p>Know if something gets lost in the mail.</p>
<hr>

<div>
  <div>
    <h2>
      Expecting a package?
    </h2>

    <form action="/insert_package" method="post">
      <div>
        <label for="description_box">
          Description
        </label>

        <input type="text" id="description_box" name="query_description">
      </div>

      <div>
        <label for="arrives_on_box">
          Supposed to arrive on
        </label>

        <input type="date" id="arrives_on_box" name="query_arrives_on">
      </div>

      <div>
        <label for="details_box">
          Details
        </label>

        <textarea id="details_box" name="query_details" rows="3"></textarea>
      </div>

      <button>
        Log delivery
      </button>
    </form>
  </div>
</div>

<hr>


<div class="waiting_on">
  <h2>Waiting on</h2>
    <ul>
      <%@list_of_unreceived_packages.each do |a_package|%>
        <li>
          <div><%=a_package.description%></div>
          <div style="color: red;">Supposed to arrive on <%=a_package.arrives_on%></div>
          <div><%=a_package.details%></div>
          <form action="/modify_package/<%=a_package.id%>" method="post">
            <button>Mark as received</button>
          </form>
        </li>
        <br>
      <%end%>
    </ul>
</div>
<hr>

<div class="received">
  <h2>Received</h2>
    <ul>
      <%@list_of_received_packages.each do |a_package|%>
        <li>
          <div><%=a_package.description%></div>
          <div>Updated <%=a_package.updated_at.strftime("%M")%> minutes ago</div>
          <a href="/delete_package/<%=a_package.id%>">Delete</a>
        </li>
        <br>
      <%end%>
    </ul>
</div>
